# English Diary App - 環境変数テンプレート
# このファイルを .env にコピーして使用してください
# cp .env.example .env

# =========================================
# データベース設定
# =========================================
DB_DATABASE=english_diary
DB_USERNAME=diary_user
DB_PASSWORD=secure_password_change_in_production

# =========================================
# WSL2環境対応（権限問題回避）
# =========================================
# WSL2では以下のコマンドで自動設定することを推奨:
# echo "USER_ID=$(id -u)" >> .env
# echo "GROUP_ID=$(id -g)" >> .env

USER_ID=1000
GROUP_ID=1001

# =========================================
# 注意事項
# =========================================
# 1. WSL2ファイルシステム (/home/) 上でプロジェクトを実行すること
# 2. /mnt/c/ 配下で実行すると権限とパフォーマンスの問題が発生します
# 3. 本番環境ではDB_PASSWORDを必ず変更してください
